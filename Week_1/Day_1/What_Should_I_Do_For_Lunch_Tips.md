### Tips 
* Try experimenting with the comparison operators(<,>,=) in the node REPL.
* Try outputting values of hungry and availableTime inside the function.

```javascript
function whatToDoForLunch(hungry, availableTime){
  console.log('hungry is', hungry);
  console.log('availableTime is:', availableTime);
}
```